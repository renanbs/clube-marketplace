---
name: init
description: Guided Clube project setup — Project Profile in CLAUDE.md & AGENTS.md, database onboarding, agent roles, memory vault, runtime validation.
---

Load and follow the skill `init` in this plugin (`skills/init/SKILL.md`).

Execute the guided onboarding: detect host, write the Project Profile to both `CLAUDE.md` and `AGENTS.md`, configure database, map agent roles from the host catalog, offer memory-graph, and validate runtimes.
